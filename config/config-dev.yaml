repositories:
  # Local repository with example photos.
  Examples:
    type: local
    root: "~/src/01memories/local/photos1"

slideshows:
  # Slideshow with example photos shown for 60 s in random sequence.
  Examples:
    repositories: Examples
    pause: 60
    order: random

# Slideshow default settings
always_excluded_tags:
  - "persönlich"
  - "Sonstiges"
  - "360"
bg_color: [ 0, 0, 0 ]
label_content: short
label_mode: auto
label_duration: 30
order: random
#orientation: landscape
pause: 300
resize: fill
rotation: 0

# Global settings
display_mode: static
enable_exception_handler: false
#enable_scheduler: true
log_level: info
#window_size: full